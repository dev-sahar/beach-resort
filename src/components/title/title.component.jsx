import React from "react";

import "./title.styles.scss";

const Title = ({ title }) => (
    <div className="section-title">
        <h4>{title}</h4>
        <div />
    </div>
);

export default Title;